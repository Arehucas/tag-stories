'use client';
import React from 'react';

export default function TestSharePage() {
  const handleShare = () => {
    // Aquí va la lógica de compartir imagen
  };
  return (
    <button onClick={handleShare} className="cursor-pointer">Compartir imagen</button>
  );
} 