import common from '@/locales/es/common.json';
import PLanding from './PLanding';

export const metadata = {
  title: common.metadata.provider_landing.title,
  description: common.metadata.provider_landing.description,
};

export default PLanding; 